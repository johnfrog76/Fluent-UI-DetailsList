import { mergeStyleSets } from "@fluentui/react";

export const buttonStyles = mergeStyleSets({ root: { marginRight: '8px' } });

export const errorLabelStyle = mergeStyleSets({ errorlabel: {color: 'red'}});